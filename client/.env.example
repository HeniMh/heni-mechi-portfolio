# Dev: leave unset — Vite proxies /api to the server (works on localhost and LAN IP).
# Production: see .env.production (Render API). Override if you use another API host:
# VITE_API_URL=https://heni-mechi-portfolio.onrender.com
